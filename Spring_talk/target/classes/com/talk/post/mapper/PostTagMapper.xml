<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http:/mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.talk.post.mapper.PostTagMapper">

<!-- 
	<select id="postTagList" resultType="com.talk.post.domain.PostTagVO">
		SELECT * FROM post_tag WHERE post_num=#{post_num}
		
		SELECT POST_NUM, TAG FROM
		(SELECT * FROM POST_TAG FULL OUTER JOIN TAG_LIST
		ON POST_TAG.TAG_NUM = TAG_LIST.TAG_NUM) WHERE post_num = #{post_num}
	</select>
	 -->
	
</mapper>