<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC	 "-//mybatis.org//DTD Mapper 3.0//EN"
	"http:/mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	<mapper namespace="com.talk.gall.mapper.GallDogReplyMapper">
	
	<select id="getList" resultType="com.talk.gall.domain.GallDogReplyVO">
		SELECT * FROM gall_dog_reply 
			WHERE board_num = #{board_num}
		ORDER BY reply_num DESC
	</select>
	
	<insert id="create">
		INSERT INTO gall_dog_reply 
		(reply_num, board_num, reply_content, writer)
			VALUES
		(reply_num.nextval, #{board_num}, #{reply_content}, #{writer})
	</insert>
	
	<update id="update">
		UPDATE gall_dog_reply 
			SET	
		reply_content = #{reply_content}, m_date = SYSDATE	
		WHERE reply_num = #{reply_num}
	</update>
	
	<delete id="delete">
		DELETE FROM gall_dog_reply 
			WHERE reply_num = #{reply_num}
	</delete>
	
	<select id ="getBoard_num" resultType="long">
		SELECT board_num FROM gall_dog_reply
			WHERE reply_num = #{reply_num}
	</select>
	
	<delete id = "deleteAllReplies">
		 delete from gall_dog_reply
		 	WHERE board_num = #{board_num}
	</delete>


</mapper>
	